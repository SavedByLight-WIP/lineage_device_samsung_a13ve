# Bluetooth vendor configuration for MediaTek
# UART device port where Bluetooth controller is attached
UartPort = /dev/ttyS1

# Firmware patch file location
FwPatchFilePath = /vendor/firmware/

# Firmware patch file
FwPatchFileName = mtk_bt_fw.bin